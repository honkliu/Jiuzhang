# Two-Host H200 Stack Setup

I have two Ubuntu machines with eight H200 GPUs each. Connect to the first machine using [paste the first SSH command here] and to the second using [paste the second SSH command here]. Use the large persistent disk at [storage path]. The services will be accessed through [public IP], and the only externally available application ports are [web/API port] and [ComfyUI port]. Before starting, ask me for any missing passwords or API keys. Internally, always discover and use the machines' hostnames rather than their private IP addresses. IPv6 does not work reliably on these machines, so use IPv4.

On the first machine, run DeepSeek V4 Pro with SGLang on all eight GPUs. Keep it in a persistent interactive Docker container named `dsk`, store its model and caches on the persistent disk, and expose its OpenAI-compatible API only on an internal port. Use the current officially recommended H200 configuration and test a real inference request; the previous setup needed a memory fraction of 0.85 because 0.90 ran out of memory. Also run LiteLLM, Open WebUI, and Caddy on this machine. LiteLLM should combine DeepSeek and Qwen behind one authenticated OpenAI-compatible API. Open WebUI should use LiteLLM and keep its user data on the persistent disk. Caddy should be the only public-facing service.

On the second machine, run Qwen 3.8 Flash Next BF16 with SGLang on GPUs 0 through 3 in a persistent interactive container named `qwen-next`. Use the current recommended multimodal, reasoning, speculative-decoding, and tool-calling configuration. Use GPUs 4 through 7 for a container named `comfyui`. Keep all ComfyUI models, workflows, inputs, outputs, caches, and user data on the persistent disk. Install the current official MiniMax H3 text-to-video, image-to-video, and reference-to-video workflows together with every dependency they require. Keep ComfyUI private and place it behind a separate Basic Auth proxy named `comfyui-auth`.

Configure LiteLLM to expose the exact model names `DeepSeek-V4-Pro-0813` and `Qwen3.8-Flash-Next`, reaching both SGLang servers through their internal hostnames. Configure Caddy so the public web/API port serves Open WebUI at its root and LiteLLM under `/v1`. Use the other public port for authenticated ComfyUI. There is no public DNS name, so use Caddy's internal certificate authority, make HTTPS work with the raw IP address, and export the root certificate for installation on client machines. Do not expose any internal service ports directly to the Internet.

Do not report success merely because the containers started. Confirm that both models complete real requests through Caddy and LiteLLM, model discovery requires authentication and lists both names, Open WebUI can chat with both models, and Qwen supports image input and structured tool calls for Open WebUI's Native/Agentic mode. Confirm that ComfyUI rejects anonymous access and that all three MiniMax H3 workflows create playable output files. Verify that Qwen and ComfyUI use separate GPU groups and that persistent data survives container recreation.

A previous MiniMax H3 run failed with `ModelSamplingAdvanced object has no attribute audio_scale`. If this happens again, resolve the compatibility mismatch between the current ComfyUI build and the official workflow, and do not call the setup complete until real video generation works. Preserve the interactive `dsk` and `qwen-next` workflow unless I request otherwise, and explain that their model processes may need to be relaunched after a host or container restart.
